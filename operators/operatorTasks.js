// https://javascript.info/operators
// task 1
// final values
// its just the prefix and postfix increment operator diffs

//task 2
// 4 and 5, no comma with assignment means no throwing away previous returned values

//task 3 
// I hate this. I so much prefer explict conversions. This is why dynamic types are gross

//task 4 fix the addition
let a = prompt("First number?", 1);
let b = prompt("Second number?", 2);

alert(a + b); // 12
//this can be fixed with the unary + to convert prompt to a numeric (+prompt)
